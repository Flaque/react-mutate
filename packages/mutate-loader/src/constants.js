// Constants
const MUTATIONS_FOLDER_NAME = "mutations";
const PACKAGE_JSON = "package.json";
const NODE_MODULES = "node_modules";
const PACKAGE_JSON_CONTENT = {
  dependencies: {}
};

module.exports = {
  MUTATIONS_FOLDER_NAME,
  PACKAGE_JSON,
  NODE_MODULES,
  PACKAGE_JSON_CONTENT
};
